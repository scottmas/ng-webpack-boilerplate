/*
describe('commonService', function(){


    it('does stuff', function(){
        module('commonService');

        inject(function($rootScope){
            console.log('before service inject', $rootScope.loaded);
        });


        inject(function($rootScope, $compile, commonService){
            console.log('after service inject', $rootScope.loaded);

        })
    });

});*/
