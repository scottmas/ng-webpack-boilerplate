/*
describe('commonDirective', function(){

    it('does stuff', function(){
        module('commonDirective');

        inject(function($rootScope, $compile){

            console.log('before directive used', $rootScope.loaded);

            var scope = $rootScope.$new();
            var element = '<div common-directive></div>';
            element = $compile(element)(scope);

            console.log('after directive used: ', $rootScope.loaded, element);
        })
    });

});*/
